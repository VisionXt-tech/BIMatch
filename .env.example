# Firebase Project Configuration
# Replace these placeholder values with your actual Firebase project credentials.
# You can find these in your Firebase project settings:
# Project settings > General > Your apps > Firebase SDK snippet > Config

# Required Firebase Configuration
NEXT_PUBLIC_FIREBASE_API_KEY="your_api_key_here"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your_project_id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your_project_id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your_project_id.firebasestorage.app"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your_sender_id"
NEXT_PUBLIC_FIREBASE_APP_ID="your_app_id"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="your_measurement_id"

# Firebase Emulators (Optional - for local development)
# Set to "true" to attempt to connect to local emulators.
# Ensure emulators are running on default ports:
# - Auth: 9099
# - Firestore: 8080
# - Storage: 9199
# - Functions: 5001
NEXT_PUBLIC_USE_FIREBASE_EMULATOR="false"

# Application Configuration
NEXT_PUBLIC_APP_NAME="BIMatch"
NEXT_PUBLIC_APP_URL="https://bimatch-cd100.web.app"

# AI Configuration (Genkit)
# Google AI API key for contract generation
# Get it from: https://makersuite.google.com/app/apikey
GOOGLE_GENAI_API_KEY="your_google_ai_api_key_here"

# Firebase Admin SDK (for server-side operations)
# REQUIRED for local development (choose ONE option):
#
# Option 1 - Use JSON file (EASIEST for development):
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key" and download the JSON file
# 3. Save it in your project root (e.g., firebase-service-account.json)
# 4. Add to .gitignore to prevent committing
# 5. Set the path below:
FIREBASE_SERVICE_ACCOUNT_KEY_PATH="./firebase-service-account.json"
#
# Option 2 - Use environment variables (for production/CI):
# Extract values from the JSON file and paste them here:
# FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBA...\n-----END PRIVATE KEY-----\n"
# FIREBASE_ADMIN_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com"

# Rate Limiting Configuration (Optional)
# Defaults are set in code, but can be overridden
RATE_LIMIT_LOGIN_MAX="5"
RATE_LIMIT_LOGIN_WINDOW_MS="900000"
RATE_LIMIT_REGISTER_MAX="3"
RATE_LIMIT_REGISTER_WINDOW_MS="3600000"

# Session Configuration (Optional)
SESSION_TIMEOUT_MS="1800000"

# Environment
NODE_ENV="development"

# IMPORTANT NOTES:
# 1. Copy this file to .env.local for development
# 2. Never commit .env.local or any file with actual credentials
# 3. For production, set these in Firebase Hosting environment config
# 4. Storage bucket must use .firebasestorage.app domain (NOT .appspot.com)
# 5. All NEXT_PUBLIC_ variables are exposed to the browser
